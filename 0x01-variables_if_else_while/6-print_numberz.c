#include <stdio.h>
/**
* main - starting point for program
* Return: 0 if no errors, non zero if error
*/

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
