#include <stdio.h>
/**
* main - starting point for program
* Return: 0 if no errors, non zero if error
*/

int main(void)

{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
